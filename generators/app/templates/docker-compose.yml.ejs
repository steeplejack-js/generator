version: '3'

services:

  app:
    build:
      context: .
    ports:<% if (server === 'express') { %>
      - '3000-3100'<% } %>
      - '5858'
      - '9999'
    volumes:
      - .:/opt/app
    command: npm run serve
