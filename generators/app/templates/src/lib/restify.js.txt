/**
 * restify
 */

/* Node modules */

/* Third-party modules */
<%- deps.importer('steeplejack-restify', '{Restify, restifyLib}') %>

/* Files */

<%= deps.exporter('default') %> () => ({
  Restify,
  restifyLib
});

<%= deps.exporter('inject') %> = {
  name: 'Restify'
};
