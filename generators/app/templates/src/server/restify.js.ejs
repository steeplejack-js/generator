/**
 * restify
 */

/* Node modules */

/* Third-party modules */

/* Files */

<%= deps.exporter('default') %> ({ Restify }) => new Restify();

<%= deps.exporter('inject') %> = {
  name: 'restify',
  deps: [
    'steeplejack-restify',
  ],
};
