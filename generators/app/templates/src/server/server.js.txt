/**
 * server
 */

/* Node modules */

/* Third-party modules */

/* Files */

<%= deps.exporter('default') %> (Server, config, { <%- deps.lodash.upperFirst(server) %> }) => {
  const <%- server %> = new <%- deps.lodash.upperFirst(server) %>();

  const server = new Server(config.server, <%- server %>);

  return server;
};

<%= deps.exporter('inject') %> = {
  name: 'server',
  deps: [
    'steeplejack-server',
    '$config',
    '<%- deps.lodash.upperFirst(server) %>'
  ]
};
