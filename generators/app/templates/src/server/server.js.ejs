/**
 * server
 */

/* Node modules */

/* Third-party modules */

/* Files */

<%= deps.exporter('default') %> (Server, config, <%= server %>) =>
  new Server(config.server, <%= server %><% if (sockets) { %>, socketIo<% } %>);

<%= deps.exporter('inject') %> = {
  name: 'server',
  deps: [
    'steeplejack-server',
    '$config',
    '<%- server %>',<% if (sockets) { %>
    'steeplejack-socketio',<% } %>
  ]
};
