version: '3'

services:

  app:
    build:
      context: .
    ports:
      - '5858:5858'
      - '9999:9999'
    volumes:
      - .:/opt/app
    command: npm run serve | ./node_modules/.bin/bunyanA
