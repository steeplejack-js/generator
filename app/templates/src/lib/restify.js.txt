/**
 * restify
 */

/* Node modules */

/* Third-party modules */<% if (compile) { %>
import {Restify} from 'steeplejack-restify';<% } else { %>
const {Restify} = require('steeplejack-restify');<% } %>

/* Files */

<% if (compile) { %>export default<% } else { %>const factory =<% } %> () => Restify;
<% if (compile) { %>
export const inject = {
  name: 'Restify'
};<% } else { %>
module.exports = {
  default: factory,
  inject: {
    name: 'Restify'
  }
};
<% } %>
